---
title: Silent Detection Configs
openapi: "POST /healthchecks/detections/silent-configs"
---

## Overview

Checks that each enabled detection config produced at least the configured minimum number of results inside the lookback window.
