---
title: Blocklist Version
openapi: "GET /asset/blocklist/version"
---

## Overview

Return the current blocklist snapshot and recent-additions feed metadata — tokens, counts, absolute CDN URLs — plus the canonicalization algorithm version. Clients use this to discover and cache blocklist artifacts rather than downloading the full list repeatedly.
