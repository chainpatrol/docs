---
title: Reviewing Backlog
openapi: "POST /healthchecks/reviewing/backlog"
---

## Overview

Counts proposals in the `PENDING` review state for the organization and grades severity
(`ok` / `warn` / `fail`) against the configurable thresholds.
