---
title: List Organization Brands
openapi: "GET /organization/brands"
---

## Overview

List every brand belonging to the organization tied to your API key. Returns parent/product brands (`type: ORGANIZATION`) and individual/employee brands (`type: INDIVIDUAL`) in one call, along with configuration such as `brandColors`, `includedTerms`, `excludedTerms`, and tickers.
